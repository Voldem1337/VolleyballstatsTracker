package com.volleyballtracker.config;

/**
 * Application configuration.
 *
 * Store common paths and constants here.
 */
public class AppConfig {

    // Folder where matches are saved as JSON.
    public static final String MATCHES_FOLDER = "src/main/resources/data/matches";

    // Folder where exported files are saved.
    public static final String EXPORTS_FOLDER = "src/main/resources/data/exports";

    // Default file extension for saved matches.
    public static final String DEFAULT_MATCH_EXTENSION = ".json";
}
